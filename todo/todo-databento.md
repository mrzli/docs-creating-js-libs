- need to have a manually specified price precision file
  - for ES/NQ/YM
  - use it when converting to normal format, so that everything is aligned
  - use it for rounding when doing backadjustments

- implement combining